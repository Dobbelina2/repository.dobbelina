name: Interactive Workflow

on:
  workflow_dispatch:
    inputs:
      my-input:
        description: 'Enter a value'
        required: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Print input value
        run: echo "Input value: ${{ github.event.inputs.my-input }}"
